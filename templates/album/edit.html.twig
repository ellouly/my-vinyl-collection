{% extends 'base.html.twig' %}

{% block title %}Modifier {{ album.name }}{% endblock %}

{% block body %}
    <div class="page-container">
        <div class="admin-container">
            <div class="title">
                <h1>MODIFIER L'ALBUM</h1>
            </div>
            <div class="row admin-widget">
                {{ include('album/_form.html.twig', {'button_label': 'VALIDER !'}) }}
            </div>
        </div>
        <div class="footer">
            <a href="{{ path('album_index') }}"><i class="las la-arrow-circle-left"></i></a>

            {{ include('album/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
