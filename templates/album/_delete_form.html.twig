<form method="post" action="{{ path('album_delete', {'id': album.id}) }}" onsubmit="return confirm('Etes-vous sûr(e) de vouloir mettre ce vinyl aux puces ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ album.id) }}">
    <button class="btn footer mt-4"><i class="las la-trash-alt"></i></button>
</form>
