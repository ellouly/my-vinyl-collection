{% extends 'base.html.twig' %}

{% block title %}{{ album.name }}{% endblock %}

{% block body %}
    <div class="bg-light">
    <h1>{{ album.name }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Année</th>
                <td>{{ album.year }}</td>
            </tr>
            <tr>
                <th>Titres</th>
                <td>{{ album.titles }}</td>
            </tr>
        </tbody>
    </table>
    <a href="{{ album.linkAudio }}">Play</a>

    <a href="{{ path('album_index') }}">Retour Jukebox</a>

    <a href="{{ path('album_edit', {'id': album.id}) }}">Modifier</a>

    {{ include('album/_delete_form.html.twig') }}
    </div>
{% endblock %}
