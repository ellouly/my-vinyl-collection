{% extends 'base.html.twig' %}

{% block body %}
    <div class="home-vinyl">
        <div class="home-title">
            <h1>MY VINYL COLLECTION</h1>
        </div>

        <div class="vinyl">
            <a href="{{ path('album_index') }}"><img src="{{ asset('build/discos-de-acetato-png-5.png') }}"></a>
        </div>
        <div class="count">
            {{ albums }}
        </div>
        <div class="footer">
            <a href="{{ path('search') }}"><i class="las la-images"></i></a>
            <a href="{{ path('record_dealer') }}"><i class="las la-headphones-alt"></i></a>
        </div>
    </div>
{% endblock %}