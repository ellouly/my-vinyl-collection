{% extends 'base.html.twig' %}

{% block title %}Modifier {{ artist.name }}{% endblock %}

{% block body %}
    <div class="page-container">
        <div class="admin-container">
            <div class="title">
                <h2>MODIFIER L'ARTISTE</h2>
            </div>
            <div class="row admin-widget">
                {{ include('artist/_form.html.twig', {'button_label': 'VALIDER !'}) }}
            </div>
        </div>
        <div class="footer">
            <a href="{{ path('artist_index') }}"><i class="las la-arrow-circle-left"></i></a>
            {{ include('artist/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
